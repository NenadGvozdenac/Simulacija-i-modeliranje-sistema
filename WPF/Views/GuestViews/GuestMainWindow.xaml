<Window x:Class="BookingApp.WPF.Views.GuestViews.GuestMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Views.GuestViews"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Title="GuestMainWindow" Height="768" Width="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!--Container for all UserControls of the app-->
        <Grid x:Name="Container" Grid.Row="0">
            <Frame x:Name="GuestWindowFrame" NavigationUIVisibility="Hidden"/>
        </Grid>

        <!--Navigation menu-->
        <Grid Grid.Row ="1" >
            <Border BorderBrush="Black" BorderThickness="0 1 0 0">
                
            </Border>

            <Button x:Name="Accommodations" Content="Accommodations" Margin="250,10,656,10" Click="Accommodations_Click"/>
            <Button x:Name="MyReservations" Content="My reservations" Margin="641,10,250,10" Click="MyReservations_Click"/>
            <Button Content="Logout" Margin="934,17,35,10" Click="Logout_Click" />
        </Grid>
    </Grid>
</Window>
